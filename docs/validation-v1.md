# Expanded v1 validation — 2026-09-08

Expanded software acceptance is recorded below. Final integrated build and hosted evidence are still pending; this is not a release-readiness claim. Only synthetic emulator records were used. Historical MVP/hosted results do not establish acceptance for the final expanded commit.

## Verified integration evidence

- Phone JVM suite: 53 tests passed. API36 phone instrumentation: 22 tests accepted across the full run and focused correction recheck. The full run passed 21 of 22; the new header-overflow assertion found that the expanded header title lacked full width. The corrected layout then passed both AdaptiveNavigationTest tests in 6.062 s. [Full run](evidence/api36-phone-instrumentation.txt) and [focused rerun](evidence/api36-header-focused.txt). Assertions were preserved. Earlier widget shell binding was corrected to use the numeric Android user.
- API26 actual APK upgrade: accepted MVP commit `2d6f2bedd592df026e5584c5d75cc0ae6bcb9e50`, versionCode 1/Room 1, was installed and populated through normal onboarding. `adb install -r` upgraded to versionCode 2/Room 2 with the same certificate, UID and first-install time. Exact DB comparison preserved the complete plan and all four IN/OUT events: current tray 3/12, 7-day interval, 1320-minute target, Europe/Rome. Only known tray 3 received actual history; earlier tray changes were not invented. New IN and a note survived restart and a subsequent replacement. [Recorded assertions](evidence/api26-upgrade-verification.txt). API36 Room migration and schema1 JSON compatibility/rejection tests also passed.
- Real bound home widget: current state renders; the actual PendingIntent changes state; repeating it adds no transition; stale revision and post-delete actions cannot change records. Reminder actions and repository validation have focused automated coverage; physical delivery remains separate.
- Photo lifecycle passes on API36 and API26: a 3200×1600 JPEG with EXIF rotation/GPS becomes an 800×1600 private JPEG without GPS. Complete archive restore validates digest/dimensions, stages new ownership, rotates generation and deletes superseded copies. Failed-unlink recovery, active-capture grace, abandoned-capture expiry and delete-all cleanup pass. API26 PhotoArchiveTest and LongHistoryTest: [2 tests passed in 7.478 s](evidence/api26-photo-crypto-tests.txt), including actual Android8 AES/PBKDF2 providers. Capture tests exercise URI/file lifecycle, not a physical camera app.
- Actual API36 encrypted SAF journey: exported a synthetic IN plan, note and private photo; changed state to OUT and added a second note. Wrong password produced an error and preserved both notes/OUT. Correct-password preview followed by Cancel also preserved them. Explicit confirmed replacement restored IN, the original note only and the visibly rendered photo. Preview included one validated photo. Screenshots: [preview](screenshots/v1/encrypted-restore-preview.png), [restored note](screenshots/v1/encrypted-restored-note.png), [restored photo](screenshots/v1/encrypted-restored-photo.png). These are interactive restore results, not conclusions inferred from codec tests.
- Actual photo comparison selected two dated cards. Exported HTML time-lapse was opened in a browser: both embedded images rendered; Play/Pause and scrub changed frames; no remote resources or console errors were observed. [Browser result](evidence/timelapse-browser.json).
- Visual review covered reachable primary action at 320dp/200% font, dark mode, short landscape scrolling, mirrored RTL layout/localized dates, and the corrected expanded header. Arabic text falls back to English; RTL evidence is not a translation claim. Full TalkBack and device accessibility remain open.
- Wear JVM suite: 8 tests passed. API36 Wear instrumentation: 4 tests passed, covering durable outbox/activity and the microG adapter against installed official Google Play services, unknown-node rejection and modern listener bindings. A separate same-node send/listener probe confirmed actual binder delivery. These do not prove paired phone/watch delivery, Tile/complication interaction on hardware or a replacement microG runtime.

Detailed synthetic UI screenshots/XML and interaction notes are retained locally in `/tmp/aligner-ui-evidence/astra-ui-acceptance.md`; upgrade APK hashes, package metadata, exact DB snapshots and screenshots are in `/tmp/aligner-upgrade-evidence/README.md`. Phone full/focused logs are `/tmp/aligner-final-full-phone.txt` and `/tmp/aligner-header-final-test.txt`; watch log is `/tmp/aligner-microg-watch-instrumentation.txt`. Committed evidence above preserves the concise portable results; these local paths are supplementary session artifacts.

## Performance evidence and limits

Debug builds on accelerated x86_64 emulators. API36 phone: two cores/3 GiB RAM; one synthetic 50,000-event run measured transactional restore 675 ms, 30-day summaries 24 ms, JSON round-trip 1693 ms, CSV 18 ms and 1 MiB encrypt/decrypt with both 600,000-round PBKDF2 derivations 4443 ms. API26 phone: two cores/2 GiB; the corresponding focused run measured 2016 ms, 22 ms, 884 ms, 20 ms and 3897 ms. Baseline and upgraded API26 cold launches were 1204 ms and 907 ms. These are observed samples, not comparable device benchmarks or physical budgets.

A Reports scroll sample contained 212 frames, 18 janky (8.49%), p50=27 ms, p90=34 ms, p95=42 ms, p99=48 ms. The GPU was emulated. KDF/image/archive processing runs off the UI thread; archive limits are 32 MiB and HTML output 16 MiB. No matched 24-hour battery or physical wakeup result is claimed.

The initial software-emulated startup ANRs were discarded as acceptance. Accelerated emulators were used without wiping existing app data; API26 upgrade used a newly created disposable AVD. No personal devices, accounts or shared runner service were changed by these acceptance lanes.

## Build, licensing and open acceptance

The resolved phone/watch runtime inventory has 171 coordinates: 169 Apache-2.0 and 2 BSD-3-Clause declarations, with no unresolved POM licenses or proprietary Google artifacts. The Apache-2.0 microG client replaced the bundled proprietary libraries; GPL-3.0-or-later is preserved. Installed Google transport and possible Google infrastructure remain separate privacy/runtime boundaries. [License and F-Droid assessment](dependency-licenses.md).

The final memory-bounded `scripts/check.sh` rerun has confirmed 53 phone and 8 watch JVM tests with zero failures/errors/skips. Refreshed lint reports have zero errors; exact remaining warning counts belong to the final generated reports. A final count-label resource correction and its focused screen recheck are being integrated. Warnings remain visible and are not waived as physical acceptance. Final release R8/build completion, artifact checksums and the hosted commit SHA/downloadable artifacts are pending reconciliation under [#23](https://forgejo.server.matejkalc.com/matejkalc/aligner-tracker/issues/23). Prior successful local checks or hosted MVP artifacts are not final expanded evidence.

Paired-emulator Wear transport remains open: no phone companion, account or interactive pairing was configured. Local official-GMS binder delivery cannot substitute for a real paired exchange. Also open: #11 physical Android/OEM/Doze/reboot/force-stop, #12 complete manual/device accessibility, #17 matched 24 h physical battery, #18 consented usability pilot, #20 physical watch and #21 user-owned signing/authorized publication. Procedures are in [external acceptance](external-acceptance.md). These gates are separate from implementation completion.
